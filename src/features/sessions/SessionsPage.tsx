import { Link } from 'react-router-dom'; import { useApp } from '../../app/AppContext'
export function SessionsPage(){const{sessions}=useApp();return <section><p className="eyebrow">LOKALES PROTOKOLL</p><h1>Letzte Versuche</h1><p className="lead">Nur auf diesem Gerät gespeichert.</p><div className="session-list">{sessions.length===0?<p className="empty">Noch keine Session gespeichert.</p>:sessions.map(s=><Link key={s.id} to={`/empfehlung/${s.id}`}><div><strong>{s.recommendations[0].spot.spot.label}</strong><p>{new Intl.DateTimeFormat('de-DE',{dateStyle:'medium',timeStyle:'short'}).format(new Date(s.createdAt))}</p></div><span>{s.feedback.length} Rückmeldungen →</span></Link>)}</div></section>}
