import { createBrowserRouter,RouterProvider } from 'react-router-dom'; import { Layout } from '../ui/components/Layout'; import { HomePage } from '../features/home/HomePage'; import { SituationPage } from '../features/situation/SituationPage'; import { RecommendationPage } from '../features/recommendations/RecommendationPage'; import { InventoryPage } from '../features/inventory/InventoryPage'; import { SessionsPage } from '../features/sessions/SessionsPage'; import { AppProvider } from './AppContext'
const router=createBrowserRouter([{path:'/',element:<Layout/>,children:[{index:true,element:<HomePage/>},{path:'neu',element:<SituationPage/>},{path:'empfehlung/:id',element:<RecommendationPage/>},{path:'koederbox',element:<InventoryPage/>},{path:'verlauf',element:<SessionsPage/>}]}])
export function App(){return <AppProvider><RouterProvider router={router}/></AppProvider>}
